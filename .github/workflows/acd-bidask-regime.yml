name: ACD BID ASK Regime

on:
  push:
    branches:
      - research/acd-execution-quick
    paths:
      - 'research/download_xauusd_bidask.js'
      - 'research/acd_bidask_regime.py'
      - '.github/workflows/acd-bidask-regime.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'research/download_xauusd_bidask.js'
      - 'research/acd_bidask_regime.py'
      - '.github/workflows/acd-bidask-regime.yml'
  workflow_dispatch:

concurrency:
  group: acd-bidask-regime
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: research/requirements-acd-quick.txt
      - name: Install Node downloader
        run: npm install --no-save dukascopy-node
      - name: Install Python dependencies
        run: pip install -r research/requirements-acd-quick.txt
      - name: Download XAUUSD BID and ASK M1
        run: node research/download_xauusd_bidask.js
      - name: Run market-regime filter test
        shell: bash
        run: |
          mkdir -p artifacts_regime
          set -o pipefail
          python research/acd_bidask_regime.py 2>&1 | tee artifacts_regime/run.log
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: acd-bidask-regime-results
          path: artifacts_regime/
          if-no-files-found: warn
          retention-days: 14
